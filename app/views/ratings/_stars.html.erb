<% split = readonly ? 4 : 2 %>
<%= radio_button_tag "rating[score]", 0.25, checked > 0 && checked <= 0.25, 
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil if readonly %>
<%= radio_button_tag "rating[score]", 0.5,  checked > 0.25 && checked <= 0.5, 
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil %>
<%= radio_button_tag "rating[score]", 0.75, checked > 0.5 && checked <= 0.75, 
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil if readonly %>
<%= radio_button_tag "rating[score]", 1,    checked > 0.75 && checked <= 1,
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil %>
<%= radio_button_tag "rating[score]", 1.25, checked > 1 && checked <= 1.25, 
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil if readonly %>
<%= radio_button_tag "rating[score]", 1.5,  checked > 1.25 && checked <= 1.5, 
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil %>
<%= radio_button_tag "rating[score]", 1.75, checked > 1.5 && checked <= 1.75, 
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil if readonly %>
<%= radio_button_tag "rating[score]", 2,    checked > 1.75 && checked <= 2, 
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil %>
<%= radio_button_tag "rating[score]", 2.25, checked > 2 && checked <= 2.25, 
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil if readonly %>
<%= radio_button_tag "rating[score]", 2.5,  checked > 2.25 && checked <= 2.5, 
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil %>
<%= radio_button_tag "rating[score]", 2.75, checked > 2.5 && checked <= 2.75, 
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil if readonly %>
<%= radio_button_tag "rating[score]", 3,    checked > 2.75 && checked <= 3,  
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil %>
<%= radio_button_tag "rating[score]", 3.25, checked > 3 && checked <= 3.25, 
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil if readonly %>
<%= radio_button_tag "rating[score]", 3.5,  checked > 3.25 && checked <= 3.5, 
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil %>
<%= radio_button_tag "rating[score]", 3.75, checked > 3.5 && checked <= 3.75, 
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil if readonly %>
<%= radio_button_tag "rating[score]", 4,    checked > 3.75 && checked <= 4, 
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil %>
<%= radio_button_tag "rating[score]", 4.25, checked > 4 && checked <= 4.25, 
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil if readonly %>
<%= radio_button_tag "rating[score]", 4.5,  checked > 4.25 && checked <= 4.5, 
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil %>
<%= radio_button_tag "rating[score]", 4.75, checked > 4.5 && checked <= 4.75, 
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil if readonly %>
<%= radio_button_tag "rating[score]", 5,    checked > 4.75 && checked <= 5, 
                      :disabled => readonly, 
                      :class => "auto-submit-star {split:#{split}}",
                      :id => nil %>
